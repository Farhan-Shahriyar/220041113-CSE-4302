#include "Coordinate.h"
#include<bits/stdc++.h>
using namespace std;

int main(){
    Coordinate coord[10];
    randomAssignment(coord, 10);
    highest(coord, 10);
    lowest(coord, 10);
}
